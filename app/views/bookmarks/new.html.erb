<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie_id, collection: Movie.all%>
  <%= f.submit %>
<% end %>
<%= link_to "Back to lists", "/lists", class: "btn btn-primary"%>
